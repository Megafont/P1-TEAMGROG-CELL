using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/// <summary>
/// This enum defines all the enemy type IDs.
/// I have set the values for each category a ways apart so there is plenty of room for each category to add IDs for its specific enemy types.
/// </summary>
public enum SpawnedUnitTypes
{
    // Tower-Spawned unit IDs
    // ==================================================
    Macrophage_Base = 0,

}